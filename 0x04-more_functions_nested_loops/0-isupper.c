#include "main.h"

/**
 * _isupper - check if its upper case
 * @x: function parameter
 * Return: return 1 for upper and 0 for any else
 */

int _isupper(int c)
{
	if (x >= 65 && x <= 90)
	{
		return (1);
	}
	return (0);
}
